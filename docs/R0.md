# NA

- Consider refactor using {data.table}
  - Need to check if I can do some sort of “grouped” correlation, that
    is the main limitation to a “long format” approach
- Can probably check different input options and turn everything into a
  format with conf.low, conf.high
